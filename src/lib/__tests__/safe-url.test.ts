import { describe,expect,it } from "vitest";import { parseSafeHttpUrl } from "@/lib/safe-url";describe("URLs externas",()=>{it("aceita HTTP e HTTPS",()=>{expect(parseSafeHttpUrl("https://example.com")?.hostname).toBe("example.com")});it("bloqueia esquemas perigosos",()=>{expect(parseSafeHttpUrl("javascript:alert(1)")).toBeNull();expect(parseSafeHttpUrl("data:text/html,x")).toBeNull()})});
